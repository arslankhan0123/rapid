<table class="table table-responsive-sm table-responsive-md table-responsive-lg table-striped table-bordered"
    id="designationTable">
    <thead class="text-center">
        <tr>
            <th scope="col">{{ __('messages.increments.increment_name') }}</th>
            <th scope="col">{{ __('messages.employees.name') }}</th>
            <th scope="col">{{ __('messages.designations.name') }}</th>
            <th scope="col">{{ __('messages.branches.name') }}</th>
            <th scope="col">{{ __('messages.increments.description') }}</th>
            <th scope="col">{{ __('messages.increments.amount') }}</th>
            <th scope="col" class="text-right">{{ __('messages.common.action') }}</th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
