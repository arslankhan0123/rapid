<table
    class="table table-responsive-xs table-responsive-sm table-responsive-md table-responsive-lg table-striped table-bordered"
    id="assetTable">
    <thead class="text-center">
        <tr>
            <th scope="col" style="display: none;"></th> <!-- Hidden column for created_at -->
            <th scope="col" class="pl-2">{{ __('messages.branches.name') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.print-checks.date') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.print-checks.check_number') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.print-checks.issue_name') }}</th>
            <th scope="col" class="pl-2">Place</th>
            <th scope="col" class="pl-2">{{ __('messages.print-checks.bank') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.print-checks.amount') }}</th>

            <th scope="col" class="text-right">{{ __('messages.common.action') }}</th>

        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
