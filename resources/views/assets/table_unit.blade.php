<table
    class="table table-responsive-xs table-responsive-sm table-responsive-md table-responsive-lg table-striped table-bordered"
    id="assetTable">
    <thead>
        <tr>
            <th scope="col" class="pl-2">{{ __('messages.branches.name') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.category_title') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.category') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.asset_company_code') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.is_working') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.assign_to') }}</th>
            <th scope="col" class="pl-2">{{ __('messages.assets.warranty_date') }}</th>
            <th scope="col" class="text-right">{{ __('messages.common.action') }}</th>
            <th scope="col" style="display: none;"></th> <!-- Hidden column for created_at -->
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
