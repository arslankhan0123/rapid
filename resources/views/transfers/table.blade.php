<table class="table table-responsive-sm table-responsive-md table-responsive-lg table-striped table-bordered"
    id="designationTable">
    <thead>
        <tr>

            <th scope="col">{{ __('messages.branches.name') }}</th>
            <th scope="col">{{ __('messages.employees.id') }}</th>
            <th scope="col">{{ __('messages.transfers.employee_name') }}</th>
            <th scope="col">{{ __('messages.transfers.from') }}</th>
            <th scope="col">{{ __('messages.transfers.to') }}</th>
            <th scope="col">{{ __('messages.common.date') }}</th>
            <th scope="col">{{ __('messages.transfers.description') }}</th>
            <th scope="col" class="text-right">{{ __('messages.common.action') }}</th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>
